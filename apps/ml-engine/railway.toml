[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["apps/ml-engine/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
numReplicas = 1
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
